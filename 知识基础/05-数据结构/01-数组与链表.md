## 数组与链表
数组与链表的基本区别
- 存储方式：
    - 数组：连续内存空间
    - 链表：分散内存空间
- 容量拓展：
    - 数组：长度不可变
    - 链表：可灵活拓展
- 内存效率：
    - 数组：元素占用内存少，但是可能浪费空间
    - 链表：元素占用内存多
- 访问元素：
    - 数组：O(1)
    - 链表：O(n)
- 添加元素：
    - 数组：O(n)
    - 链表：O(1)
- 删除元素：
    - 数组：O(n)
    - 链表：O(1)